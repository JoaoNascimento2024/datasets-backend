/**
 * Configurações globais para a API.
 * 
 * @type {Object}
 */
const config = {
    /**
     * Versão atual da API, usada para gerenciar as rotas e a compatibilidade da versão.
     * @type {string}
     */
    versaoAPI : "v1"
};

export default config;
